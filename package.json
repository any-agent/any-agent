{
  "name": "any-agent",
  "private": true,
  "version": "0.0.1",
  "description": "",
  "license": "MIT",
  "type": "module",
  "packageManager": "bun@1.3.2",
  "workspaces": {
    "packages": [
      "packages/*",
      "packages/tools/*"
    ],
    "catalog": {
      "@ai-sdk/anthropic": "^2.0.44",
      "@ai-sdk/openai": "^2.0.65",
      "@types/bun": "1.3.2",
      "@tsconfig/bun": "1.0.10",
      "ai": "5.0.93",
      "@biomejs/biome": "^2.2.6",
      "typescript": "5.9.3",
      "zod": "4.1.12"
    }
  },
  "scripts": {
    "build": "bun run --filter '*' build",
    "build:web": "bun run --filter '@any-agent/web' build",
    "build:core": "bun run --filter '@any-agent/cor' build",
    "dev": "bun run --filter '@any-agent/web' dev",
    "test": "bun run --filter '*' test",
    "typecheck": "bun run --filter '*' build:tsc",
    "lint": "bun run --filter '*' lint",
    "prepare": "husky"
  },
  "devDependencies": {
    "@biomejs/biome": "catalog:",
    "@tsconfig/bun": "catalog:",
    "@types/dockerode": "^3.3.45",
    "husky": "^9.1.7",
    "typescript": "catalog:"
  }
}
